import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#555',
    paddingVertical: 2,
    paddingHorizontal: 6,
    borderRadius: 8,
  },
  text: {
    fontSize: 12,
    color: '#FFF',
  },
});

export { styles };
